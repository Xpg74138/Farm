[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "9"

[context.production]
  environment = {
    NODE_ENV = "production"
  }
